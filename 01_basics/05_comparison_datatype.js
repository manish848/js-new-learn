console.log(null < 0); // all false
console.log(null == 0); 
console.log(null > 0); 
console.log(null >= 0); // this is true, as the comparison operator always convert it to the number and the number of niull is zero so it gives us the true answer, we need to remeber this one always
console.log(null <= 0); // true


console.log(null < undefined); 
console.log(null > undefined); 
console.log(null  == undefined); // undefined and null are considered equal


console.log(2 == "0002");  // try to chane it in the number form

// strict check in teh javascript , how the strict check in the javascript works, and how we can do that
 // ===  this type of the check not only do the comparison of the values but also the data typ so it is good to use

 